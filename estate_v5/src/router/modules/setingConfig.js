import BasicLayout from '@/pages/settingConfig'

const meta = {
  auth: true,
  title: '$t:globalConfiguration',
}

export default {
  path: '/settingConfig',
  name: 'settingConfig',
  meta,
  component: BasicLayout,
}
